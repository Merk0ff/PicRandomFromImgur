from setuptools import setup, find_packages

setup(
    name='PicRandomFromImgur',
    version='1.0',
    author='Philip merk0ff',
    packages=find_packages(),
    install_requires = [
        'beautifulsoup4'
    ]
)
